---
task_id: AC-012
title: Execute one task through the integrated durable local workflow
status: review
priority: high
risk: medium
depends_on: [AC-011]
branch: feat/AC-012-integrated-workflow
owner: codex
last_updated: 2026-09-14
qa: required
deployment: not_applicable
pull_request: required
---

# Outcome

An operator runs one prepared task through scoped planning, implementation, deterministic verification, independent review, bounded fixes, explicit QA, and configured completion evidence using a resumable CLI command.

## Why now

All eleven foundation tasks are merged, including PR #13 corrections. The PRODUCT MVP milestone still lacks a connected fixture journey.

## Required context

- `docs/PRODUCT.md`: AC-001 through AC-011 and completion gates.
- `docs/WORKFLOW.md`: lifecycle, freshness, optional phases, and resume.
- `docs/ARCHITECTURE.md`: file state and narrow adapters.
- `docs/SECURITY.md`: untrusted results, confinement, and secrets.
- `docs/RELEASE.md`: fixture and package evidence.
- `docs/DECISIONS.md`: local CLI and explicit QA decisions.
- `SYSTEM.md`: implemented boundaries and remaining integration gap.

## Scope

### In

- A durable workflow API and CLI run/resume interface using existing subprocess and policy boundaries.
- Separate planning, implementation, verification, and structured independent review; bounded fix rounds with fresh downstream evidence.
- Explicit operator QA and external completion policy; missing remote adapters block rather than imply completion.
- Commit, task, configuration, and workspace freshness validation on resume; conservative reconciliation for interrupted model effects.
- Fixture success, failure, forced interruption, and resume evidence.
- Documentation refresh and a requirement-by-requirement MVP code audit.

### Out

- GitHub push/merge or deployment automation, hosted services, direct model APIs, package publication, and adjacent release implementation.

## Implementation constraints

Reuse existing containment, redaction, durable locks, and retry budgets. Preserve unrelated work. Work only in the declared linked feature worktree. Generate the implementation plan against current HEAD before coding. Model claims cannot pass deterministic or external gates.

## Required execution sequence

1. Create and verify the feature worktree from main.
2. Refresh the plan against code and commit identity.
3. Implement, verify, independently review, fix, reverify, and run fixture QA.
4. Push and open the required PR after local gates pass.
5. Await human-authorized merge gates before marking done and moving this contract.

## Done when

- [x] One fixture completes the integrated run with retained per-phase evidence.
- [x] Forced interruption/resume does not repeat a completed model effect.
- [x] Failed checks, actionable review, missing QA, stale identity, and missing external gates prevent completion.
- [x] Fix ceilings survive restart and changes require fresh checks/review/QA.
- [x] Documentation and an MVP requirements audit accurately distinguish implemented, verified, and unreleased behavior.

## Deterministic validation

- `pnpm format:check`
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test` (serialize files if Windows lock contention requires it)
- `pnpm build` and built CLI help/fixture smoke

## Independent critical-review focus

Challenge resume freshness, interrupted model reconciliation, review schema failures, evidence forgery/staleness, fix budgets, path confinement, secret retention, and external completion claims.

## QA

Required: exercise the integrated CLI fixture journey, failed check, missing gates, and interrupted resume. Record subprocess evidence. No UI exists.

## PR, merge, and production gates

- PR applicability: required.
- Codex PR review: required.
- Merge authorization: human.
- Production verification: not applicable; local source/CLI change without deployment.
- Local verification and applicable QA must precede publication; commit, push, and PR creation do not require a separate manual or external review. The owner accepts this chat as AC-012's independent review. Exact-head remote review and owner authorization remain merge gates.

## Files/areas expected

`src/cli.ts`, `src/workflow.ts`, subprocess adapters and tests, `docs/`, `SYSTEM.md`, `tasks/README.md`.

## Manual owner steps or blockers

Codex authentication is needed for live model smoke; human merge authorization and release identity/license remain owner decisions. Remote workflow effects require explicit integrations and cannot be invented by model output.

## Completion record

### Yarn Classic and pip credential coverage, 2026-09-14

Security finding 4003280205 also identifies ignored virtualenv pip.ini credentials. Shared discovery now isolates pip.ini/pip.conf and applies existing INI scalar redaction and raw-byte freshness. Code-review finding 4003178000 identifies the distinct ignored Yarn Classic .yarnrc filename. Shared discovery now recognizes Classic and modern Yarn files. Literal Classic key/value and assignment syntax, quoted values and comments participate in scalar redaction; raw-byte hashing detects changes. Unsupported quoted syntax fails before launch with a generic error. Existing native credential isolation coverage includes Classic. Containment version 10 rejects v9 and earlier receipts.

Fresh frozen-source verification covers all 310 cases: 306 pass, four platform skips, zero failures/cancellations; all 13 source files and 33 workflow roots are covered exactly once. The other 12 files run serialized, forced interruption runs alone, then three disjoint workflow groups run. Every final process exits zero without retry. Hash-identical production-JavaScript Windows boundary QA passes 56 tests; built pnpm CMD and CLI run/resume QA passes two. Disposable production controls reproduce Classic and pip credential reads; the corrected build denies both and still denies nested prior-run evidence. Configured formatting, lint, typecheck, clean build and built CLI help pass. Frozen source hashes match. Ignored proof is retained under .autocode/implementation-plans/AC-012-pr14-classic-final-*. AC-012 remains in review; exact-head remote reviews and human merge gates remain required. Broader MVP acceptance is unclaimed.

### Yarn credentials and private durable evidence, 2026-09-14

Security findings 4002943864 and 4002943867 identify ignored Yarn registry tokens and prior durable evidence exposed to sandboxed commands. Shared credential classification now recognizes .yarnrc.yml/.yaml; existing YAML scalar redaction and raw-byte freshness hashing apply, with explicit token/redaction/freshness and native denied-read coverage. Durable AutoCode metadata and every descendant are private to the trusted host, with credential ACL preflight and exact restoration. The driver supplies phase prompts/inputs directly; sandboxed commands cannot read earlier run transcripts. Public common Git metadata keeps its explicit role even when named .autocode. Existing read-only package grants on actual state refuse launch unchanged. The metadata-drift fixture uses an ordinary operator signal rather than reading private configuration. Containment version 9 covers these changes and literal SSH refusal, invalidating v8 and earlier receipts. Disposable controls reproduce both Yarn-token and nested prior-run evidence exposure on the earlier build.

Fresh combined frozen-source verification covers all 310 cases: 306 pass, four platform skips, zero failures/cancellations, all 13 source files/33 workflow roots exactly once. Other files run serialized; forced interruption runs alone, followed by three disjoint workflow groups. All final commands exit zero without retry. Production-JavaScript Windows boundary QA passes 56 tests; built pnpm CMD and CLI run/resume QA passes two. Disposable production proofs deny the Yarn token and nested prior-run evidence, and refuse the glob-based RSA-key reference preflight. Configured formatting, lint, typecheck, clean build and built CLI help pass with matching frozen source hashes. Superseded/failing controls remain explicitly historical. Exact-head remote reviews and human merge remain gates.

### Literal SSH syntax enforcement, 2026-09-14

PR finding 4002881520 demonstrates escaped-space and glob identity references that bypass literal-only isolation. SSH config commands now receive a single-pass conservative literal-character and balanced-quote check. Escaping, expansion, shell metacharacters, nonliteral paths and unbalanced quotes fail before launch; separate/joined literal -i and IdentityFile forms remain covered. Backslash/Unicode command layouts are unsupported and fail closed. No shell is interpreted or expanded by discovery. Private reference inspection also skips out-of-scope paths before lstat and refuses links before canonical resolution. Containment version 9 invalidates v8 and earlier receipts. Native regressions exercise escaped spaces, globs, environment substitutions, tilde/command substitution, operators and unbalanced quotes, preserving ACLs/contents and proving no process ran. A disposable generated RSA-key glob control reproduces the prior exposure.

Superseded pre-publication verification was stopped after the final security review identified the Yarn and durable-evidence findings. The latest checkpoint above supplies fresh combined-source proof; prior results are not reused.

### SSH identities and executable resource privacy, 2026-09-14

PR finding 4002683253 demonstrates that core.sshCommand can reference opaque private keys through -o IdentityFile= or joined -oIdentityFile= forms. Literal argument inspection now handles these forms alongside separate/joined -i, option quoting, value quoting and case-insensitive IdentityFile keys without executing or expanding commands. Native regressions protect every referenced target in current/additional repositories, preserve public config reads and ordinary writes, and restore original ACLs/contents. Containment version 8 invalidates v7 and earlier receipts.

Security finding 4002714550 confirms the same SSH-option gap. Unsupported SSH -F configuration indirection now fails before launch. Distinct finding 4002714553 identifies implicit executable/shim parent-directory read grants; selection now grants only the selected file, with no recursive parent grant or automatic DLL copying. Additional runtime files require explicit host authorization and unsupported layouts fail closed. Native executable/shim controls reproduce colocated credential exposure on the earlier build; fixed cases preserve credential privacy, ACLs, contents and ordinary writes. The pnpm fixture keeps its helper inside the authorized package/worktree scope.

Fresh final frozen-source verification covers all 310 cases: 306 pass, four platform skips, zero failures/cancellations, all 13 files and 33 workflow roots exactly once. Other files run serialized, the forced-interruption fixture runs alone, and the remaining workflow roots run in three disjoint processes. All final commands exit zero without retry. Production-JavaScript Windows boundary QA passes 56 tests; built pnpm CMD and CLI run/resume QA passes two. The generated RSA SSH-option reference is protected by the final build. Configured formatting, lint, typecheck, clean build and built CLI help pass with matching frozen hashes. Earlier failing controls and superseded verification remain explicitly historical; exact-head remote reviews and human merge remain gates.

### Private Git file references and explicit metadata roles, 2026-09-14

Independent critical review reproduced an opaque generated RSA private key exposed through http.sslKey despite isolating its config. Known Git TLS key/certificate, cookie, credential-file, literal askpass, SSH identity and credential-store references now receive bounded canonical discovery and credential isolation inside authorized resources. References do not execute helpers, read private file contents or authorize external resources. PR finding 4002440974 additionally showed a custom common directory named .autocode; discovery now carries its explicit Git metadata role independently of the basename, including recursive module privacy. New regressions preserve public parent config/refs, ordinary writes, original contents and ACL restoration. Containment version 7 invalidates v6 and earlier receipts.

Fresh frozen-source verification covers all 306 cases: 302 pass, four platform skips, zero failures/cancellations and every one of 13 source files/33 workflow roots exactly once. The forced-interruption fixture runs alone; other files are serialized and the remaining workflow roots run in three disjoint processes. Every command exits zero, with no retry required. Fresh production-JavaScript boundary QA passes 52 tests; built pnpm CMD and CLI run/resume QA passes two. The real disposable RSA-key regression now reports PRIVATE_REFERENCE_PROTECTED. Configured formatting, lint, typecheck, clean build and built CLI help pass; source hashes match. Current-head remote review and human merge remain gates.

### Submodule metadata privacy, 2026-09-13

PR #14 security finding 4002215127 on 216c1a4 identifies credentials in standard submodule configs and included fragments under common Git modules metadata. The complete modules namespace is now private by default, including nested submodules, opaque filenames and nested reserved metadata. Every descendant receives credential ACL preflight/isolation; a preexisting package-read grant on an included fragment blocks launch unchanged. No filename or config-include inference authorizes private submodule reads. Git commands requiring submodule metadata can therefore be unavailable; no private-data fallback exists.

Native regressions cover current/additional repositories and registered linked worktrees, included config fragments, nested module URL credentials, opaque files, denied reads, ordinary authorized writes, readable parent Git configs/refs and exact ACL/content restoration. Published-source controls reproduce all four cases; the fixed five-test group passes. Containment version 6 invalidates v5 and earlier receipts. The owner-accepted chat supplies scoped independent critical review.

Referenced includes are isolated even when they leave the modules subtree. The guarded Git reader uses --no-includes; a separate bounded, deduplicated walk parses only regular referenced files inside resources authorized for sandbox access, including chained and conditional includes. Includes do not authorize external host reads or grants. Every existing in-scope referenced file remains private, cycles terminate through canonical inspection caching, and more than 1,000 include targets or unsafe inspection fails closed without raw config values. A module-only control reproduces outside-fragment exposure; the complete fixed config/include group passes 12 native tests.

Fresh final-source coverage verifies all 304 cases: 300 passed and four platform skips, covering all 13 source test files and all 33 workflow registrations. Other files run serialized and workflow tests run in three isolated, disjoint Node processes. The initial parallel run passed 299 cases with four skips and one forced-interruption-fixture failure at its 30-second outer process budget. With all competing QA finished, that sole failed fixture passes in isolation; source, assertions and timeouts are unchanged. The original failed attempt remains intact. `.autocode/implementation-plans/AC-012-pr14-modules-verified-coverage.json` maps every workflow root to its successful evidence and records the failed attempt plus retry explicitly; raw suite/manifest/process logs and `AC-012-pr14-modules-interruption-retry.log` are retained. Compiled Windows boundary QA passes 50 tests and compiled pnpm CMD/CLI run-resume QA passes two, using hash-identical production JavaScript. Configured formatting, lint, typecheck, clean production build and built CLI help pass. Frozen source hashes match. Earlier aborted/module-only runs do not support this checkpoint.

AC-012 remains in review under exact-head external review and human merge gates; this does not claim broader MVP acceptance.

### Historical Git metadata authorization and config isolation, 2026-09-13

PR #14's P1 findings (4001840321 and 4001840327) on e951cce are corrected. An external common Git directory requires original explicit directory read authorization or a registered linked worktree whose canonical back-pointer resolves to this workspace's .git file. Derived read grants do not authorize another repository. Unrelated pointers, including pointers to another registered worktree, fail before launch grants. Actual registered feature worktrees remain supported.

Bounded Git config parsing does not follow includes. Config/config.worktree files with URL user information, auth headers, credential settings, private references, includes or embedded helper commands receive credential isolation; public configs and valueless booleans remain readable. Git inspection errors retain no raw output that could include secrets. Credential-bearing configs may prevent commands that require those files; no secret-bearing config or authentication fallback is exposed. Containment version 5 invalidates v4 and earlier receipts.

Positive controls on e951cce reproduce both bypasses in disposable fixtures. Native regressions reject unregistered/wrong-worktree pointers with no child launch and unchanged ACLs, permit explicitly authorized reads, deny private config reads in current/additional repositories, preserve exact config contents/ACLs and retain public config reads. The owner-accepted chat supplies independent critical review.

Native Codex fixture defaults now allow Windows host startup while retaining explicit short-timeout tests and production limits. Detailed failure reproduction showed the prior two-second fixture budget expired before artifact/duplicate/orphan/overflow assertions; all five focused assertions pass after this correction. Public refs and worktree identifiers named config are regression-covered as data labels; only actual Git config locations are parsed.

Final root review also protects directly authorized .credentials/.secrets directories and their opaque files, includes core.askPass authentication helpers in config isolation, and limits writable .autocode enclaves to registered worktrees under .autocode/worktrees with external common metadata. Plain Git markers cannot reclassify cache/run state. Native old-source controls reproduce these gaps; fixed cases retain registered infrastructure and preserve original ACLs/content.

Fresh verification on the frozen final source: all 13 source test files and all 33 workflow test registrations are covered exactly once. The complete source suite passes 295 tests with four platform skips and zero failures/cancellations; other files run serialized and workflow tests run in three isolated, disjoint Node-process shards. Compiled Windows boundary QA passes 45 tests, and compiled pnpm CMD/CLI run-resume QA passes two. Configured formatting, lint, typecheck, clean production build and built CLI help pass. Source hashes match the frozen snapshot. Ignored evidence is retained in `.autocode/implementation-plans/AC-012-pr14-git-suite-results.json`, its manifest and per-process logs, and `AC-012-pr14-final-built-host.log` / `AC-012-pr14-final-built-workflow.log`. One earlier unchanged-source attempt encountered a Windows EBUSY fixture-cleanup error; the complete same-source rerun passes. Earlier aborted or superseded runs do not support this checkpoint.

This checkpoint supersedes earlier metadata read authorization and config coverage. AC-012 remains in review under exact-head remote review and human merge gates; broader MVP acceptance remains unclaimed.

### Historical additional writable-root boundary corrections, 2026-09-13

PR #14's P1 credential-root finding (4001625111) and P2 metadata-root finding (4001625113) on c76db07 exposed additional paths to the same isolation boundary. Before any launch grants, discovery now covers every writable root, including sibling/parent resources, nested repositories and linked common Git metadata. Case aliases and covering roots are deduplicated. Shared credential classification protects direct cloud credential directories and private metadata stores; all reserved .git/.autocode descendants receive protected metadata boundaries. Roots inside protected metadata fail closed before launch. Public Git data labels remain readable and ordinary authorized files remain writable. Complete traversal is bounded to 100,000 entries, with a 10,000-entry generic-resource limit. Containment version 4 rejects v3 and earlier receipts.

Native regressions cover sibling, parent, nested, linked and case-alias roots; deny credential read/write/rename/delete and metadata write/rename/delete; preserve exact original ACLs and contents; and verify safe ordinary writes and public Git reads. Additional cases protect a directly authorized cloud directory and reject metadata-root overlap without launching. Positive controls against c76db07 reproduced the original credential/metadata bypasses in disposable fixtures. The owner-accepted chat supplies independent critical review.

Fresh verification over c76db07: the complete serialized source suite passes 279 tests, with four platform-specific skips and zero failures; no tests are omitted. Compiled Windows boundary QA passes 29 tests, and compiled pnpm CMD/CLI run-resume QA passes two. Configured formatting, lint, typecheck, clean production build, built CLI help and diff checks pass. Frozen source hashes remain unchanged. Evidence is retained under gitignored .autocode/implementation-plans/AC-012-pr14-extra-*.log.

This checkpoint supersedes containment version 3's writable-root coverage. AC-012 remains in review under exact-head remote review and human merge gates. Existing command/platform and live-authentication acceptance gaps remain recorded below; complete MVP acceptance is not claimed.

### Historical credential and Git inspection boundary corrections, 2026-09-13

The latest P2 ACL-cleanup finding (3999340870) and P1 common-credential-filename finding on PR #14 are corrected. Shared discovery covers dotenv, npm/netrc/pypirc/Git auth, auth JSON/YAML, private keys and sensitive cloud/SSH/Kubernetes/Docker paths. Raw-byte fingerprints detect binary changes; bounded format-aware redaction also handles padded YAML tokens. Generic non-repository launches scan recursively with a 10,000-entry ceiling and fail closed on credential links or overflow.

Credential isolation never removes and reconstructs preexisting package/capability allows. Such access blocks launch and requires operator hardening, leaving those ACL entries unchanged. Normal and timeout cleanup remove only launch-specific permissions and restore inheritance only when the recorded temporary boundary is unchanged; concurrent hardening remains authoritative. Containment version 3 invalidates earlier receipts.

Critical review found that trusted-host Git inspection could execute text-conversion, clean/process-filter and filesystem-monitor helpers outside containment and conceal changes. Every production Git inspection call now disables these helpers, including smudge filters and external diff drivers. A regression includes actual unsafe positive controls, confirms no helper executes through AutoCode, and verifies hidden edits invalidate evidence.

Critical review also reproduced preexisting package write access on a descendant of protected metadata. Recursive metadata ACL preflight now rejects package/capability write/delete/ACL-changing rights, null ACLs and links, with a 100,000-entry ceiling before enqueue. Safe package read access remains allowed; regression verifies read access, write denial, unchanged original ACLs and fail-closed unsafe launch.

Fresh verification of the corrections over 9f1e4e1: the complete serialized source suite outside the restricted Windows sandbox passes 271 tests, with four platform-specific skips and zero failures; no tests are omitted. Compiled Windows boundary QA passes 21 tests, and compiled pnpm CMD/CLI run-resume QA passes two. Configured formatting, lint, typecheck, clean build, built CLI help and diff checks pass. The source hashes remain identical to the frozen verification snapshot. Evidence is retained under gitignored .autocode/implementation-plans/AC-012-pr14-*.log.

The owner-accepted chat supplies independent critical review, including the reproduced Git-helper and padded-token findings and their corrections. Exact-head remote PR review and human merge authorization remain gates; AC-012 stays in review. Production verification is not applicable. Live authenticated Codex compatibility, captured Node child-process pipes, cross-volume batch execution, unsupported platforms and broader MVP acceptance remain unaccepted; no raw-process fallback is enabled. This checkpoint supersedes earlier claims of reconstructing original package ACL grants.

### Credential file and environment isolation correction, 2026-09-13

The two P1 findings on 5980b70 (review comments 3999275845 and 3999275850) are corrected. Before every shared Windows launch, discovered ignored credentials lose sandbox package/capability access, including preexisting broad application-package grants. Operator permissions remain available and original ACL entries and inheritance are restored after termination. CreateProcess receives an explicit Unicode minimal system/runtime environment with private home/profile/temp directories; operator tokens, provider keys and unrelated variables are omitted. Legacy Job-only and credential-exposing AppContainer receipts cannot advance upgraded runs. No raw-process or inherited-environment fallback is enabled.

Regressions deny reads of .env, .credentials.json and nested credentials, verify absence of synthetic operator tokens, and prove exact original ACL restoration including prior ALL APPLICATION PACKAGES access. Privileged concurrent-writer and synthetic-output controls retain credential-fingerprint and redaction coverage without a production bypass. The full serialized source suite passes 264 tests with four platform skips and zero failures; compiled Windows boundary checks pass 15 and compiled pnpm CMD/CLI run-resume QA passes two. Formatting, lint, typecheck, clean build, built CLI help and diff checks pass. Evidence is retained under gitignored .autocode/implementation-plans/AC-012-credential-*.log. This owner-accepted chat supplies independent critical review; exact-head remote review and human merge authorization remain gates.

Implicit CLI authentication is unavailable; explicit credential-reference authentication and live authenticated Codex compatibility remain acceptance gaps. Existing platform and command gaps remain documented below; complete PRODUCT acceptance is not claimed.

### Windows broker and metadata isolation correction, 2026-09-13

PR #14's P1 Windows broker finding on 80e8e9b is corrected with a unique AppContainer granting internetClient only, retaining suspended-start Job Object lifetime containment. A real Task Scheduler fixture denies registration and launch of an existing operator-owned task; the identical trusted-parent launch produces its harmless marker as a positive control. Additional host resources require explicit authorization, helper code and cleanup manifests remain outside writable resources, and no uncontained fallback is enabled.

Critical review also closes metadata mutation before an operator crash: sandbox write grants are removed from .autocode, .git and common Git metadata before execution. Temporary inheritance boundaries retain operator permissions and restore the original ACLs after termination. Regressions cover six metadata write/delete/rename attacks, ordinary new-file create/read/edit, exact ACL restoration and legacy Job-only receipt rejection. Existing terminal-tamper tests retain privileged-writer controls without a production bypass.

Fresh verification of these corrections over 80e8e9b: the complete serialized suite outside the restricted Windows sandbox passes 263 tests, with four platform skips and zero failures. Compiled Windows boundary checks pass 14; compiled pnpm CMD and CLI run/resume fixtures pass two. Formatting, lint, typecheck, clean build, built CLI help and diff checks pass. Evidence is retained under gitignored .autocode/implementation-plans/AC-012-appcontainer-*.log. The owner-accepted chat supplies independent critical review and dispositions the finding with deterministic evidence. Exact-head remote review and human merge authorization remain gates.

Current Node/libuv cannot create captured child-process pipes inside AppContainer; commands requiring that behavior fail closed at configured limits. Cross-volume batch execution and live authenticated Codex compatibility remain unaccepted. Linux/macOS and broader PRODUCT acceptance gaps remain explicit in MVP_AUDIT.md; this correction does not claim complete MVP acceptance.

### Windows batch-shim and architecture corrections, 2026-09-13

The two findings on 423c466 are corrected. Windows CMD/BAT package-manager shims launch through a trusted cmd.exe interpreter inside the same suspended-start Job Object. Arguments are quoted and escaped for both interpreter passes, delayed expansion is disabled, and NUL/line-break arguments fail before launch. Native executable behavior and UTF-8 stdin remain. ARCHITECTURE now matches SECURITY/WORKFLOW: Windows execution is enabled; Linux and macOS fail closed pending accepted containment and PRODUCT's three-platform requirement remains open.

CMD and BAT regressions preserve spaces, Unicode, quotes, trailing backslashes, percent/exclamation variables, metacharacters, empty arguments and stdin, reject line breaks/NUL, and verify no injected marker appears. A configured command: pnpm CMD-shim workflow completes and resumes without repeated model work. The host/boundary suite passes 12; Fresh verification over 423c466: the complete serialized suite outside the restricted Windows sandbox passes 259 tests with four platform skips and zero failures. Compiled batch/boundary checks pass 11; compiled pnpm workflow and CLI run/resume pass two. Formatting, lint, typecheck, clean build, built CLI help and diff checks pass. README and SYSTEM also match the disabled Linux path. Evidence is retained under gitignored .autocode/implementation-plans/AC-012-batch-shims-*.log. The owner accepts this chat as independent review; exact-head remote review and human merge authorization remain required.

### Linux user-manager boundary correction, 2026-09-13

Fresh review of 0ec0fb5 found that a Linux subprocess can request a surviving sibling transient service from the user manager. Linux execution now fails closed before any process launch or workflow-history acceptance; macOS also remains unavailable. The unsafe Linux guardian/unit launcher and the raw Codex runner are removed. All Codex sessions use the contained runner; workflow, QA, Codex and verification entry points enforce platform preflight. Windows Job Object execution remains enabled. PRODUCT's three-platform requirement remains unmet and is recorded in MVP_AUDIT.md; this correction does not claim full MVP acceptance.

A regression simulates Linux and macOS, checks every execution boundary plus resume preflight, and verifies no subprocess marker is created. Windows host/argument/timeout/output/lifetime tests continue to pass. Fresh verification over 0ec0fb5: the complete serialized suite outside the restricted Windows sandbox passes 255 tests with four platform skips and zero failures. The boundary/Windows host suite passes nine; compiled boundary/host checks pass eight; built CLI run/resume passes. Formatting, lint, typecheck, clean build, built CLI help and diff checks pass. Evidence is retained under gitignored .autocode/implementation-plans/AC-012-linux-*.log. the owner accepts this chat as independent review, while exact-head remote review and human merge authorization remain required.

### Codex and verification descendant containment correction, 2026-09-12

Fresh Codex review of 12b92f8 found the same orphaned-helper escape in integrated implementation and fix roles. Integrated planning, implementation, review and fix roles now use the shared contained subprocess runner, with UTF-8 prompt input supplied through the trusted host. Deterministic check commands use the same boundary, closing the equivalent repository-command escape. Windows Job Object assignment precedes execution and descendant termination precedes protected-state checks. Linux retains systemd cgroup/operator-identity containment; Linux runtime is not verified on this Windows host. Conservative reconciliation and existing terminal tampering checks remain.

Regression cases exercise implementation, fix and verification roots that create an orphaned detached grandchild waiting for blocked completion, and include a viable uncontained event-forgery control. Fresh verification over 12b92f8: the full serialized suite outside the restricted Windows sandbox passes 252 tests with four platform skips and zero failures. The restricted run reproduced the documented concurrent-initialization failure and was stopped; concurrent initialization passes in the complete external rerun. Focused escape regressions pass four; compiled QA host checks pass five; built CLI run/resume passes. Formatting, lint, typecheck, clean build, built CLI help and diff checks pass. Evidence is retained under gitignored .autocode/implementation-plans/AC-012-codex-containment-*.log. Windows standalone Codex sessions also use the Job Object path. The owner accepts this chat as independent review; fresh exact-head Codex review and human merge authorization remain gates.

### QA descendant containment correction, 2026-09-12

The fresh Codex finding on dc8647a is corrected by containing QA execution before trusting durable state. Required integrated QA accepts only frozen, worktree-bound adapters from createContainedQaAdapter; arbitrary in-process callbacks fail preflight before model effects. Windows starts the command suspended, assigns it to a kill-on-close Job Object before execution, waits for all descendants to terminate before returning output, and watches the operator process lifetime. Linux retains systemd cgroup containment with a separate operator-identity guardian. Timeout and output overflow fail closed. Standalone runQaPhase remains a trusted callback policy helper, not a process boundary.

The regression starts an orphaned detached grandchild that waits for genuinely blocked completion and appends valid forged durable events. Containment prevents the append and resume stays blocked; a control runs the identical script outside containment and proves the forged history is accepted. Windows host regressions cover detached grandchildren, operator death, bounded timeout/output, and preserved Unicode/quoted arguments. Plain callback preflight and conservative missing-adapter recovery pass. Linux runtime containment is not verified on this Windows host. Fresh verification over dc8647a: the full serialized suite passes 248 tests with four platform skips and zero failures. The final strengthened attack/recovery regressions pass two; compiled QA host checks pass five, and the built CLI run/resume fixture passes. Formatting, lint, typecheck, clean build, built CLI help and diff checks pass. Evidence is retained under gitignored .autocode/implementation-plans/AC-012-contained-qa-*.log. This owner-accepted review chat dispositions the finding with evidence; exact-head remote review and human merge authorization remain gates.

### Completion reconciliation and policy preflight corrections, 2026-09-12

The two latest findings are corrected. Completion is excluded from mutable receipt-only reconciliation, including blocked receipts changed to applied and interruption after publication of passing completion evidence. Operator reconciliation is required; only durably completed gate evaluation advances. All supplied nested QA, PR-exception and completion sections are validated before preparation or model effects, using the shared QA schema and pure completion evaluator. Invalid policies create no bound workflow or model effects and can be corrected on the same commit.

Fresh verification over 484832b: the full serialized suite passes 240 tests with four platform skips and zero failures; focused completion/policy regressions pass 12. Formatting, lint, typecheck, clean build, built CLI help and built CLI run/resume pass. Logs are retained under gitignored .autocode/implementation-plans/AC-012-completion-policy-*.log. This owner-accepted review chat dispositions both findings with evidence. Fresh exact-head Codex review and human merge authorization remain gates.

### Mutable QA receipt reconciliation correction, 2026-09-12

The latest P1 finding is corrected conservatively: a QA receipt cannot establish completion of callback descendants. Resume never reconciles an in-flight QA effect as applied from its mutable receipt, including interruption after a passing receipt was published. Only an already completed durable QA phase advances; interrupted or blocked callbacks require operator reconciliation and are not repeated. Missing-adapter recovery remains available only for an attempt that ran no callback and produced no QA receipt. Late ignored credential changes invalidate freshness on resume.

Regressions forge a blocked QA receipt and its scenario evidence into passing values, interrupt after publishing a passing receipt, and change credentials after QA returns. Fresh verification over 5e10c51: full serialized suite 228 passed, four platform skips, zero failures; focused QA/recovery suite six passed. Formatting, lint, typecheck, clean build, built CLI help and built CLI run/resume pass. Logs are retained under gitignored .autocode/implementation-plans/AC-012-qa-reconcile-*.log. The owner-accepted review chat dispositions the finding with evidence. Fresh exact-head Codex review and human merge authorization remain required.

### Fresh receipt and verification credential corrections, 2026-09-12

The two follow-up findings on PR #14 are corrected. Fresh phase execution rejects pre-created receipts; reuse is limited to reconciliation of already in-flight effects, and atomic publication collisions are terminal tampering. Every verification command checks ignored credential paths and content hashes before evidence is accepted. Tampering retains failed evidence and prevents later commands or gates.

Regressions inject valid and malformed completion receipts after QA checks and receipt publication, and cover unchanged, modified, deleted, added and newly introduced ignored credentials, including an unsuccessful verification command. Fresh verification over fa667e9: the full serialized suite passes 224 tests with four platform skips and zero failures. Formatting, lint, typecheck, clean build, built CLI help and built CLI run/resume pass. Logs are retained in gitignored .autocode/implementation-plans/AC-012-final-gate-*.log. The owner-accepted chat review dispositions both findings with regression evidence. Fresh exact-head remote review and human merge authorization remain required.

### Terminal Codex tampering correction, 2026-09-12

The follow-up P1 finding on PR #14 is corrected: all Codex role exits receive protected-state and credential checks, including unsuccessful processes. A classified tampering error becomes a terminal durable failure before receipt reconciliation. No trusted phase receipt is saved from poisoned state. Regression cases forge downstream receipts from implementation and fix roles with both successful and failed exits, restore the prior workspace to defeat stale-workspace checks, and confirm resume stays failed without model calls or new durable events. Fresh verification over 78ea1c0: full serialized suite 214 passed, four platform skips, zero failures; focused tampering suite five passed. Formatting, lint, typecheck, clean build, built CLI help and built CLI run/resume pass. Logs are retained under gitignored .autocode/implementation-plans/AC-012-terminal-*.log. This owner-accepted chat review dispositions the finding with regression evidence. Exact-head remote review and human merge authorization remain required.

### PR review corrections, 2026-09-12

The three Codex findings on PR #14 are corrected: receipts redact payload text before JSON serialization; review verdicts are parsed from bounded raw output before display redaction; QA checks ignored credential hashes and detects new or deleted credential files. Credential changes also invalidate evidence on resume. Regression coverage exercises secrets colliding with booleans, JSON keys and review controls, a review/fix/review journey, unchanged and modified/deleted/added QA credentials, and credential changes after paused QA.

Fresh verification over `b15f9eb`: the full serialized suite passes 209 tests with four platform-specific skips and no failures. Formatting, lint, typecheck, clean build, built CLI help, and a separate built CLI run/resume fixture pass. Logs are retained in gitignored `.autocode/implementation-plans/AC-012-pr-review-*.log`. This owner-accepted review chat dispositions all three findings with regression evidence; the corrected head requires fresh remote review. Earlier receipts using the worktree-only fingerprint are conservatively stale and require a new run. The task remains in review pending exact-head remote review and human merge authorization.

### Review corrections, 2026-09-12

Published as [PR #14](https://github.com/hcuddeback/autocode/pull/14) from `feat/AC-012-integrated-workflow`. Commit `1980bca` contains the verified corrections and owner-authorized publication policy changes. The owner accepts this chat as the independent review; both findings are resolved. Required exact-head Codex PR review and human merge authorization remain pending, so the task stays in review.

The two local review findings are corrected in the declared feature worktree: deterministic commands check all protected state and detected tampering produces a durable terminal failure; required QA records a separate immutable preflight receipt when its adapter is absent and resumes only the matching unstarted attempt after the adapter is supplied. QA callbacks also receive protected-state checks. Regression fixtures cover forged review/current-verification receipts, receipt mutation/deletion, refusal to complete poisoned runs after restart, missing-adapter recovery, and refusing to repeat an interrupted subsequent QA callback.

Verification of the working tree corrections over `9ad1e81`: the full serialized suite in the restricted Windows sandbox passed 201 tests with four platform-specific skips and no failures. Focused regressions: seven passed. Formatting, lint, typecheck, clean compilation, built CLI help, and a built CLI run/resume fixture pass using Node.js 24.19.0 and the locked repository tools. Evidence logs are retained under the feature worktree's gitignored `.autocode/implementation-plans/`. These source changes supersede the older verification claims below. The owner accepts this chat as the independent review: both findings are resolved with regression evidence. The owner explicitly authorized committing, pushing, and PR creation and adopted D-005 so publication does not require a separate manual review. The task remains in review pending required PR/merge gates.

### Historical implementation verification

Verification refreshed 2026-09-12 against implementation commit `1509c66`: the full serialized suite outside the restricted Windows sandbox passes 193 tests with four platform-specific skips and no failures, including CLI fixture QA and forced-interruption resume. Formatting, lint, typecheck, compilation, built CLI help, and diff checks pass using the existing locked repository tools and Node.js 24.19.0. No source changes were needed. The restricted test run reproduced the known concurrent-initialization failure and was stopped before the successful full rerun. Independent external review remains pending explicit source-sharing authorization; publication has not occurred.

Branch: `feat/AC-012-integrated-workflow`, based on main `63e8a49`, in the isolated `.autocode/worktrees/ac-012-integrated-workflow` checkout. Implementation acceptance and local QA pass. Full serialized suite: 189 passed, four platform-specific skips, no failures. Final affected workflow rerun: 13 passed. Focused explicit durable outcomes, resume-only refusal, and CLI run/resume tests pass; the verification suite rerun passes 12 with two platform skips. Locked-tool formatting, lint, typecheck, clean build, and built CLI help pass with Node.js 24.19.0; no lockfile changes were made.

At that historical checkpoint, automatic approval review rejected authenticated external Codex review because source-sharing authorization was not explicit, and the branch had not been pushed or published as a PR. The owner's subsequent acceptance of this chat as the independent review and D-005 supersede that publication blocker. Exact-head Codex PR review and human-authorized merge gates remain required. Production verification is not applicable because this change has no deployment. `docs/MVP_AUDIT.md` records the remaining complete-MVP gaps; no complete release is claimed.
